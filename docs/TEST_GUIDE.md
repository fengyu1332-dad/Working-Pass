# 📋 用户系统完整测试指南

## 📑 目录
- [测试前准备](#测试前准备)
- [测试环境检查](#测试环境检查)
- [用户注册功能测试](#用户注册功能测试)
- [登录认证功能测试](#登录认证功能测试)
- [点数购买功能测试](#点数购买功能测试)
- [深度报告获取测试](#深度报告获取测试)
- [PDF下载功能测试](#PDF下载功能测试)
- [常见问题排查](#常见问题排查)

---

## 🧪 测试前准备

### 1. 安装依赖
```bash
cd /workspace
npm install @supabase/supabase-js
pip install -r requirements.txt
```

### 2. 检查环境配置
确保以下文件存在且配置正确：
- [js/auth.js](file:///workspace/js/auth.js) - Supabase认证模块
- [js/payments.js](file:///workspace/js/payments.js) - 支付模块
- [js/reports.js](file:///workspace/js/reports.js) - 报告模块

### 3. 启动本地服务器（可选但推荐）
```bash
# 使用Python启动简易服务器
python3 -m http.server 8000

# 或使用Node.js
npx serve -l 8000
```

---

## ✅ 测试环境检查

### 运行系统验证脚本
```bash
node verify_system.js
```

这个脚本会检查：
- ✅ `reports` 表是否正常
- ✅ `user_profiles` 表是否正常  
- ✅ Storage存储桶是否配置
- ✅ `download_records` 表是否正常

### 检查报告源文件
```bash
ls -la /workspace/data/reports/
```

应该看到：
- 11个专业的 `.txt` 文件
- 11个专业的 `.pdf` 文件

---

## 👤 用户注册功能测试

### 测试步骤

1. **访问注册页面**
   - 打开浏览器访问 `http://localhost:8000/register.html`
   - 或者使用GitHub Pages地址

2. **测试正常注册**
   - 输入手机号：`13800138000`
   - 输入密码：`Test123456`
   - 确认密码：`Test123456`
   - 点击"获取验证码"（演示环境）
   - 输入验证码：`123456`
   - 点击"注册"按钮

3. **验证注册结果**
   - ✅ 应该跳转到登录页或用户中心
   - ✅ 数据库中应该有新的用户记录
   - ✅ `user_profiles` 表中应该有对应记录

4. **测试边界情况**
   - ❌ 输入无效手机号（如11位以下）
   - ❌ 输入密码不匹配
   - ❌ 输入已存在的手机号

### 测试代码示例（可选）
```javascript
// 在浏览器控制台中测试认证模块
auth.initSupabase();
// 检查是否有当前用户
const user = await auth.getCurrentUser();
console.log('当前用户:', user);
```

---

## 🔐 登录认证功能测试

### 测试步骤

1. **测试正常登录**
   - 访问 `http://localhost:8000/login.html`
   - 使用刚才注册的账号登录
   - 登录成功后应该跳转到首页或用户中心

2. **测试登录状态持久化**
   - 登录成功后刷新页面
   - ✅ 应该保持登录状态
   - 在导航栏应该能看到用户信息

3. **测试登出功能**
   - 点击导航栏的"退出"按钮
   - ✅ 应该清除登录状态
   - ✅ 应该跳转到登录页

4. **测试错误登录**
   - 输入错误密码
   - ❌ 应该显示错误提示

### 验证要点
- [ ] 登录成功后自动跳转
- [ ] 刷新页面保持登录状态
- [ ] 登出后正确清除会话
- [ ] 错误登录有友好提示

---

## 💰 点数购买功能测试

### 测试准备

1. **访问购买页面**
   - 登录后访问 `http://localhost:8000/user/purchase.html`

2. **检查初始点数**
   - 新用户点数余额应该是 `0`

### 购买流程测试

1. **选择套餐**
   - ✅ 套餐列表正常显示
   - ✅ 价格和点数正确对应

2. **模拟支付**
   - 选择任意套餐（如10点套餐）
   - 点击"立即购买"
   - ✅ 应该创建订单记录
   - ✅ 模拟支付成功后，点数应该增加

3. **验证点数变化**
   - 购买前：记录当前点数
   - 购买后：刷新页面检查点数是否增加
   - ✅ 点数应该正确累加

4. **查看订单历史**
   - 访问 `http://localhost:8000/user/orders.html`
   - ✅ 应该显示刚才的订单
   - ✅ 订单状态应该是"已支付"

### 测试数据验证

在Supabase Dashboard中检查：
```sql
-- 检查订单记录
SELECT * FROM orders WHERE user_id = '你的用户ID';

-- 检查用户点数
SELECT points_balance FROM user_profiles WHERE id = '你的用户ID';
```

---

## 📚 深度报告获取测试

### 测试前置条件
- ✅ 用户已登录
- ✅ 用户点数余额 ≥ 1

### 完整流程测试

1. **访问报告列表**
   - 访问 `http://localhost:8000/user/reports.html`
   - ✅ 报告列表正常加载
   - ✅ 显示报告预览内容
   - ✅ 显示"已下载"标记（如果已购买）

2. **查看免费预览**
   - 点击任意报告卡片
   - ✅ 显示预览内容
   - ✅ 显示"解锁完整报告"按钮

3. **解锁完整报告**
   - 点击"消耗1点解锁完整报告"
   - ✅ 点数应该减少1点
   - ✅ 显示完整报告文本
   - ✅ 应该看到"下载PDF版"按钮

4. **验证已下载**
   - 关闭弹窗重新打开
   - ✅ 应该直接显示完整内容，不再扣点
   - ✅ 在列表页显示"已下载"标记

5. **返回检查**
   - 访问首页任意专业的"雪峰点评"
   - 点击"获取深度分析报告"
   - ✅ 应该跳转到报告页面

### 数据库验证

```sql
-- 检查下载记录
SELECT * FROM download_records WHERE user_id = '你的用户ID';

-- 检查报告下载计数
SELECT major_name, download_count FROM reports;
```

---

## 📄 PDF下载功能测试

### 测试步骤

1. **确保已解锁报告**
   - 先按照上面的步骤解锁一个报告

2. **测试PDF下载**
   - 打开已解锁的报告详情
   - 点击"📄 下载PDF版"按钮
   - ✅ 应该开始下载PDF文件
   - ✅ 下载的文件可以正常打开
   - ✅ 中文显示正常（文泉驿字体）

3. **验证签名URL**
   - 打开浏览器开发者工具
   - 查看Network请求
   - ✅ PDF链接应该是Supabase签名URL
   - ✅ URL包含有效期信息（1小时）

4. **测试过期处理**
   - 等待1小时后（可选）
   - 再次尝试下载
   - ✅ 应该重新生成签名URL

### PDF文件检查清单

- [ ] 文件大小正常（应该在100KB - 500KB之间）
- [ ] 使用PDF阅读器可以正常打开
- [ ] 中文显示正常，没有乱码或方块
- [ ] 页眉页脚显示正确
- [ ] 目录导航正常
- [ ] 格式排版美观

---

## 🔍 完整测试用例表

| # | 测试项 | 测试步骤 | 预期结果 | 状态 |
|---|--------|---------|---------|------|
| 1 | 用户注册 | 输入手机号、密码注册 | 注册成功，创建用户profile | ⬜ |
| 2 | 用户登录 | 使用注册账号登录 | 登录成功，保持会话 | ⬜ |
| 3 | 用户登出 | 点击退出按钮 | 登出成功，清除会话 | ⬜ |
| 4 | 查看点数 | 访问用户中心 | 显示当前点数余额 | ⬜ |
| 5 | 购买点数 | 选择套餐并购买 | 点数增加，订单记录生成 | ⬜ |
| 6 | 查看订单 | 访问订单历史页 | 显示购买记录 | ⬜ |
| 7 | 浏览报告 | 访问报告列表 | 显示所有专业报告 | ⬜ |
| 8 | 查看预览 | 点击报告卡片 | 显示报告预览内容 | ⬜ |
| 9 | 解锁报告 | 点击解锁按钮 | 扣点成功，显示完整内容 | ⬜ |
| 10 | 已下载标识 | 返回列表 | 显示"已下载"标记 | ⬜ |
| 11 | 重复解锁 | 再次解锁同一报告 | 不重复扣点 | ⬜ |
| 12 | 点数不足 | 点数为0时解锁 | 提示点数不足 | ⬜ |
| 13 | PDF下载 | 点击下载按钮 | 正常下载PDF | ⬜ |
| 14 | PDF质量 | 检查PDF文件 | 中文正常，格式美观 | ⬜ |
| 15 | 首页跳转 | 从首页点击获取报告 | 跳转到报告页面 | ⬜ |

---

## 🛠️ 常见问题排查

### 问题1：认证失败
**症状**：无法登录或注册
**排查步骤**：
```javascript
// 在浏览器控制台运行
auth.initSupabase();
const user = await auth.getCurrentUser();
console.log(user); // 查看是否有用户信息
```
**解决方案**：检查Supabase URL和Key是否正确配置

### 问题2：报告无法加载
**症状**：报告列表显示"暂无数据"
**排查步骤**：
```bash
# 运行验证脚本
node verify_system.js
```
**解决方案**：运行数据导入脚本
```bash
node import_reports_to_db.js
```

### 问题3：PDF下载失败
**症状**：点击下载按钮没有反应
**排查步骤**：
1. 检查Storage桶是否创建
2. 检查文件是否已上传
3. 检查浏览器控制台错误

**解决方案**：上传PDF文件
```bash
node upload_reports_to_storage.js
```

### 问题4：点数没有变化
**症状**：购买后点数没有增加
**排查步骤**：
1. 检查 `orders` 表记录
2. 检查 `user_profiles` 表
3. 检查RLS策略是否正确

---

## 📊 测试报告模板

测试完成后，请填写以下信息：

```
🗓️ 测试日期：2026-05-23
👤 测试人员：___________

✅ 通过的测试：____ / 15
❌ 失败的测试：____

主要问题：
1. _________________________
2. _________________________

改进建议：
1. _________________________
2. _________________________
```

---

## 🎯 快速测试命令

### 1. 一键环境检查
```bash
cd /workspace
node verify_system.js
```

### 2. 数据准备（如果需要）
```bash
node import_reports_to_db.js
node upload_reports_to_storage.js
```

### 3. 查看现有文档
```bash
cat DEPLOYMENT_GUIDE.md
cat README.md
```

---

## 📞 技术支持

如果遇到问题：
1. 查看浏览器开发者工具的Console错误
2. 查看Network请求状态
3. 检查Supabase Dashboard的RLS策略
4. 参考 [DEPLOYMENT_GUIDE.md](file:///workspace/DEPLOYMENT_GUIDE.md)

---

**祝测试顺利！** 🎉
